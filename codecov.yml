github_checks:
  annotations: false

component_management:
  individual_components:
    - component_id: mcp_server_browser
      name: mcp-server-browser
      paths:
        - packages/agent-infra/mcp-servers/browser/src/**
    - component_id: neura_action_parser
      name: neura-desktop-action-parser
      paths:
        - packages/neura-desktop/action-parser/src/**
    - component_id: neura_sdk
      name: neura-desktop-sdk
      paths:
        - packages/neura-desktop/sdk/src/**
